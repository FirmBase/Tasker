# Tasker

## Build Schema

Execute SQL script in `Countries.sql` and `Tasker.sql`.

## Intial Configuration

Change appropriate changes in Configuration.java.

## Build

Build using command

mvn.cmd package -f pom.xml

## Deploy in tomcat

Placed Tasker.war file in Tomcat/webapps.
